# web admin login (http://localhost:8480/admin), kept as a recovery account after the setup
STALWART_RECOVERY_ADMIN=admin:admin
# used by data/stalwart/entrypoint.sh on the first start
MAIL_HOSTNAME=mail.example.test
MAIL_DOMAIN=example.test
MAIL_USER=dev
MAIL_USER_PASSWORD=dev-password-123
