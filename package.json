{
  "name": "docker",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "ad": "docker compose up -d adminer",
    "all": "docker compose --profile tools --profile observability --profile kafka --profile search up -d",
    "certs": "./certs/generate_ca.sh",
    "cloud": "docker compose --profile cloud up -d nextcloud nextcloud-cron",
    "down": "docker compose --profile '*' down",
    "home": "docker compose --profile tools up -d homepage",
    "kafka": "docker compose --profile kafka up -d kafka kafka-ui",
    "kc": "docker compose up -d keycloak",
    "kong": "docker compose up -d kong",
    "kong-setup": "docker compose up -d kong-setup",
    "listmonk": "docker compose --profile listmonk up -d listmonk mailpit",
    "logs": "docker compose logs -f",
    "ls": "docker compose up -d localstack",
    "mail": "docker compose --profile mail up -d mailpit stalwart docker-mailserver mailu-front mailu-admin mailu-imap mailu-smtp mailu-antispam mailu-webmail postal-web postal-smtp postal-worker plunk listmonk postfix opensmtpd",
    "mailcow": "scripts/mailcow.sh",
    "mailserver": "docker compose --profile mailserver up -d docker-mailserver mailpit",
    "mailu": "docker compose --profile mailu up -d mailu-front mailu-admin mailu-imap mailu-smtp mailu-antispam mailu-webmail mailpit",
    "media": "docker compose --profile media up -d jellyfin navidrome",
    "obs": "docker compose --profile observability up -d",
    "pg": "docker compose up -d postgres",
    "photos": "docker compose --profile photos up -d immich-server immich-machine-learning",
    "plunk": "docker compose --profile plunk up -d plunk",
    "portainer": "docker compose --profile admin up -d portainer",
    "postal": "docker compose --profile postal up -d postal-web postal-smtp postal-worker mailpit",
    "ps": "docker compose ps",
    "relay": "docker compose --profile relay up -d postfix opensmtpd mailpit",
    "search": "docker compose --profile search up -d opensearch opensearch-dashboards",
    "selfhosted": "docker compose --profile selfhosted up -d",
    "smarthome": "docker compose --profile smarthome up -d homeassistant mosquitto",
    "sq": "docker compose up -d sonarqube",
    "stalwart": "docker compose --profile stalwart up -d stalwart mailpit",
    "tools": "docker compose --profile tools up -d",
    "up": "docker compose up -d"
  }
}
